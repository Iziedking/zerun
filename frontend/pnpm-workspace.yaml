# Build-script policy for pnpm 11. These optional native addons are pulled in
# transitively by the wagmi connectors and are not needed for the browser build,
# so their build scripts are intentionally not run.
allowBuilds:
  bufferutil: false
  keccak: false
  utf-8-validate: false
